# Local Development Configuration
# Copy this file to .env.local for local environment variables
# cp .env.example .env.local

# Database - Neon PostgreSQL via Vercel
# These environment variables are automatically provided by Vercel when you link Neon
# For local development, get these values from your Vercel project settings
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Authentication (future use)
# AUTH_SECRET=your_auth_secret
